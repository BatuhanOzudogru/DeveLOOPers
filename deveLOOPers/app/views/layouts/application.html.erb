<!DOCTYPE html>
<html>
  <head>
    <title>DeveLooPers</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 font-sans leading-normal tracking-normal">
    <nav class="bg-red-800 p-4 shadow-md">
      <div class="container mx-auto flex items-center justify-between">
        <a href="/" class="text-black text-2xl font-semibold">DeveL∞Pers</a>
        <div class="flex items-center space-x-4">
          <% if user_signed_in? %>
            <span class="text-black">Logged in as <strong><%= current_user.email %></strong></span>
            <%= link_to "Edit profile", edit_user_registration_path, class: "text-black hover:bg-red-700 px-3 py-2 rounded" %>
            <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-black hover:bg-red-700 px-3 py-2 rounded" %>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path, class: "text-black hover:bg-red-700 px-3 py-2 rounded" %>
            <%= link_to "Login", new_user_session_path, class: "text-black hover:bg-red-700 px-3 py-2 rounded" %>
          <% end %>
        </div>
      </div>
    </nav>

    <main class="container mx-auto mt-10 px-5">
      <%= yield %>
    </main>
  </body>
</html>
